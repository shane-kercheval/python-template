[project]
name = "template"
version = "0.1.0"
description = "A template project."
requires-python = ">=3.13"

dependencies = [
    "click",
    "coverage",
    "notebook",
    "numpy",
    "pandas",
    "pytest",
    "pytest-asyncio",
    "python-dotenv",
    "PyYAML",
    "ruff",
]
